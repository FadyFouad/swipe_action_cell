name: swipe_action_cell_example
description: Example application for the swipe_action_cell package.
version: 1.0.0
publish_to: none

environment:
  sdk: ">=3.4.0 <4.0.0"
  flutter: ">=3.22.0"

dependencies:
  flutter:
    sdk: flutter
  swipe_action_cell:
    path: ../

dev_dependencies:
  flutter_test:
    sdk: flutter
