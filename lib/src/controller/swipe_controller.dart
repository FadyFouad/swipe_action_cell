import 'package:flutter/foundation.dart';

/// Controller for programmatic interaction with a [SwipeActionCell].
///
/// Reserved for future use (F007).
class SwipeController extends ChangeNotifier {
  /// Creates a [SwipeController].
  SwipeController();
}
